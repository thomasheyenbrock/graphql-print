{
  "name": "graphql-print",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "benchmark": "node -r esbuild-register test/benchmark.ts",
    "build": "tsup src/index.ts --format esm,cjs --dts --minify --external graphql",
    "format": "prettier --write '**/*.{js,ts,json,md}'",
    "test": "vitest"
  },
  "peerDependencies": {
    "graphql": ">=15.0.0"
  },
  "devDependencies": {
    "@types/benchmark": "^2.1.2",
    "benchmark": "^2.1.4",
    "esbuild": "^0.17.6",
    "esbuild-register": "^3.4.2",
    "prettier": "^2.8.4",
    "tsup": "^6.5.0",
    "typescript": "^4.9.5",
    "vitest": "^0.28.4"
  }
}
